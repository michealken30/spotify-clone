---
apiVersion: v1
kind: Service
metadata:
  name: spotify-app
  labels:
    app: spotify-app
spec:
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: spotify-app
  type: LoadBalancer
